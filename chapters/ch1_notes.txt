shinyapp+shift+tab    creates shiny boilerplate env
  
Run App               Use a keyboard shortcut: Cmd/Ctrl + Shift + Enter.

fluidPage()           A layout function that sets up the basic visual structure of the page. 
  
reactive({})          A reactive expression has an important difference: it only runs the first time it is called and then it caches its result until it needs to be                         updated. 